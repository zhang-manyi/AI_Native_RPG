# 唯一候选冻结与验证范围

候选在 candidate-selection.json 中记录哈希；开发和留出均使用同一文件。
只补公开事实 ID 契约，不改变主模板、行动策略、生成模型或表达开关。

开发三个公开案例均不再虚构第二个 fact ID；仍有两次多查关系，按原标准
公开题全部参数质量仅从 3/6 到 3/5，全参数正确回合从 0/3 到 1/3。
**不是公开任务已解决**：年龄答案从 1/3 降到 0/3；三次仍调整关系后重写，
一次漏年龄、一次“小托比七岁”、一次“艾拉八岁”。不能将这个窄候选推广。

开发两步移动两条件各三次仍拒绝第一步；完整指定拒绝覆盖均 0/6。
因此按照预先协议继续执行一次新留出验证，保留所有失败，不重选第二候选，
也不为拒绝覆盖强迫完整 Harness 提议行动。这个实验可验证局部改动的泛化边界，
不声称已经满足采用门槛。

候选选择记录在开发基线之后、开发候选之前创建；本说明在开发候选完成后整理。
留出只做最终验证，后续如用其失败调优，它将转为开发数据并需要新的留出。
