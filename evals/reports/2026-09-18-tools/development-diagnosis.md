# 开发基线完成后的单因素选择

`dev-baseline-authorized/report.json` 三次重复完成 24/27 输出；3 次缺失均为
玛尔塔未执行第一步移动，第二步按协议跳过。51 HTTP，105,852 token，无缺失 usage。
沙箱首次尝试另存 dev-baseline：一次 ConnectError、其余未执行，不是模型能力分。

指定公开查询的两次运行先后或同批调用了 `victim_name` 与凭空补出的
`victim_age` / `missing_child_age`。三个 victim_name 返回都含“艾拉，十四岁”，
多查的 ID 正常返回 known=false：**执行成功，参数选择失败**。
第三次额外改查私人记忆。公开案例参数正确 3/6；全参数正确回合 0/3。
开发中“笔记本叫石榴”的首次召回均命中，模型也均没有重复查询，但三次
adjust_relationship 后重写台词均未答名，属于既有表达证据丢失/回避，不在本候选修改范围。
工具补查三次找回会面地点并答出广场，但附加不存在的钟楼/座位/交谈情景。
六个完整指定拒绝机会均变成调整关系；局部六次真实拒绝，不冒充完整覆盖。

只选一个问题：**公开事实查询把属性名臆造为新的记录 ID**。
最小候选在 npc_planning.txt 的 check_public_fact 说明后补充条目 ID 契约：
使用已经给出的 ID，不为姓名/年龄等属性编造 ID；同一返回可含多项信息。
不变更工具执行、模型、检索器、循环预算、行动、Validator 或表达路径。
候选通过现有 PromptLibrary overlay 装载；未采用前不修改游戏默认模板。

主要指标冻结为公开查询的参数正确率与全部参数正确回合率。
额外私人记忆查询仍按原标准算无关调用，不因候选范围窄而放宽。
完整任务成功、安全、拒绝覆盖、完成率与成本继续按 protocol.md 作采用门槛。
预期这只修复一个查询问题，不预期同时解决玛尔塔出门、私密披露或重写丢失答案。

初始基线全部输出已由助手阅读；语义标签会绑定每个台词的 SHA-256 保存，
原始输出及规则分数不改写。
