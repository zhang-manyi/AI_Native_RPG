# 公开表达边界修复：运行前协议

起点HEAD 5d3d51347900ed7c0f487e60096e965140e8c6b9，包含此前未提交工作区。
原失败证据：../2026-09-18-demo-acceptance/authorized-run/report.json 步骤05/07/11。
不改历史报告、不commit/push；不新增游戏功能、Agent、Judge或训练。

## 修复边界

私有规划、检索器、工具和行动策略不变。复用已存在的来源投影，但不直接开启旧候选：
新public_expression路径补充结构化动作类型/真实结果，引导表达仅使用明确已结算的
summary和npc_reply，不接收原始constraints、reasoning、strategy或私密persona。
无行动、批准、拒绝及已结算选项都必须经过公开表达边界；旧实验分支及其默认关闭状态保留。
实测通过前只用显式实验配置；通过后启用真实Web和终端入口。mock脚本保持离线展示，
新路径另用真实Harness+可控客户端进行契约测试，不把mock路线视为新边界效果证据。

## 验证路线（实现前冻结）

route-original.json逐字复制之前的22步路线，不改失败输入。
route-variant.json采用此前协议中未运行的留出变体：玩家布袋叫“河岸”，存档后追问布袋名，
酒馆问“你说的送货人是在关门前还是关门后看见她的？”。其余操作相同。
变体为新输入组合，不是独立故事；不能根据变体输出继续调优后仍称留出。
模型不读取本协议、评分标准或复核标签。每条台词逐条助手复核，不用关键词当语义评分。

采用门槛：两条路线均真实完成并到truth_uncovered，模型输出缺失/作者兜底为0；两次恢复
保持世界与全部NPC记忆/对话一致，拒绝与终局限制正常；无未授权案件披露、严重案件编造、
虚报执行成功。原话追问须正确回述；公开顺序回答须依据可见资料，资料不足时明确不确定，
不强求回答未知钟点或关门前后。一般修辞单独记录，不以小样本成功证明整体安全。

## 预算和比较

沿用现有DeepSeek配置、温度0.7、实际Web检索装配（上一有效块为Qwen3 256维），
默认工具上限、客户端重试/超时不变；输出上限1200与上一完整验收相同。
每条最多50HTTP / 180,000 token / 600秒（请求前截止，有超时尾部）；两条有效路线
总100HTTP / 360,000 token / 1200秒。最多两次针对已定位问题的代码修复，且不增加
真实路线预算；若变体失败，不继续拿变体调优。基础设施连接失败保留记录，可授权后
对未完成块重跑一次，失败未知usage保留预算预留。
token预留规则沿用TaskTransport，货币成本未知记null。新增表达成本按调用分支解释；
由于对话会改变后续记忆/状态，不把不同整局请求总数的差异都归因为单一调用的开销。

离线回归覆盖私有信息隔离、同玩家原话来源、partial投影、工具伪造、批准/拒绝/无行动、
明确已结算结果、表达失败后继续与不重掷检定；重要运行时变化需剧情和Web相关回归。
不能只复跑原失败题声称泛化。若未达采用门槛，保留候选关闭及全部证据，明确阻塞。
